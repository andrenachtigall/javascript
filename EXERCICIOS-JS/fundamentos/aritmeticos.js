const [a, b, c, d] = [3, 5, 1, 15]

const soma = a + b + c + d // infix
const substracao = d - b
const multiplicacao = a * b
const divisao = d / a
const modulo = a % 2

console.log(soma, substracao, multiplicacao, divisao, modulo)
//d++ // post fix
//+dd // pre fix
